# AVE Agent
# Core
httpx>=0.27.0           # Async HTTP for tool API calls

google-genai>=1.0.0     # Gemini 2.5 Flash video captioning
fal-client>=0.7.0       # fal.ai file upload + queue-backed media tools

# System dependency (not pip):
#   ffmpeg + ffprobe must be installed:
#     brew install ffmpeg   (macOS)
#     apt install ffmpeg    (Linux)
